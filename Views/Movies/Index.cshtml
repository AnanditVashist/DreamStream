@model IEnumerable<DreamStream.Models.Movie>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>
@foreach (var movie in Model)
{
<ul>
    <li>@Html.ActionLink(movie.Name,"Details",new { id = movie.Id })-@movie.Genre.Type</li>
</ul>
}
